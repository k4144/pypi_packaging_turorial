def divide_by_three(num: float) -> float:
    """Divide the input by three.

    Args:
        num (float): Value to divide.

    Returns:
        float: ``num`` divided by three.
    """
    return num / 3
